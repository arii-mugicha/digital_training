# AWSのVPC空間の作成手順(一例)

## VPCの構築
- VPCを作成
    - VPCの名前を設定: 管理の容易化
    - 仮想的にマシンを作成するとみなしても良い
- プライベートアドレス空間として占有する
    - CIDRを設定することで、アドレス定義
- VPC内の設定に関してはVPCダッシュボードから飛べる

## IGW (Internet Gate Way) の設定
- インターネットとVPCの接続口
- 名称を設定して、VPCに接地(アタッチ)
    - Attachedになっていることを確認

## サブネットの作成
- VPC内にサブネットを作成する
    - 特定のアドレス空間を分割し、区画に名称を付ける
    - 後のセキュリティグループの作成に利用可能
- サブネット名を指定
- アベイラビリティゾーンを指定
- IP VPC CIDRブロックがVPCのものであることを確認
- IP サブネットCIDRブロックを指定して、サブネットを分割

## ロールの作成
- エンティティタイプ、ユースケースを設定
- 許可ポリシーを選択
- ロール名、説明を付与して作成

## セキュリティグループの作成
- サブネットに対して通信(in-out)の許可を設定する
- VPCを選択して、許可を行う通信のルールを設定
    - インバウンド:サブネットに入ってくる通信
    - アウトバウンド:サブネットから出ていく通信

## EC2インスタンスの立ち上げ
- パブリックIPを割り当てないと接続が不可能になる。
- ロールとセキュリティグループをアタッチして権限、アクセス管理
- キーペアを作成するとSSH接続に利用可能
- ネットワーク設定でVPC、サブネット、パブリックIPの割り当て、セキュリティグループ、ロールなどを設定

## ルートテーブルの作成と設定
- VPCに作成
    - デフォルトでVPCのネットワークアドレスが送信先設定されている
- ルートの編集によってIGWを設定可能
    - 送信先: 最終的に何処に送るのか
    - ターゲット: 次の送り先(経由先)
- サブネットを関連付ける
    - サブネットにルートテーブルの設定を紐づける

## Session Manager
EC2インスタンスの内部にアクセスして弄れる。